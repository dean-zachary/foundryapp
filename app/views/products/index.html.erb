<body>
<div class="container">
  <p id="notice"><%= notice %></p>
</div>

  <h1 class= "text-center">Our Bikes!</h1>

<!-- <table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
      <th>Image url</th>
      <th>Color</th>
      <th>Price</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <%= render @products %>
  </tbody>
</table>  -->

<div class="container">
  <div class="row">
    <% @products.each do |product| %>
      <div class="col-sm-4 col-md-4">
       <div class="thumbnail">

          <%= link_to (image_tag(product.image_url, class: "img-responsive .center-block")), product %>
          <div class="caption">
            <h1 id="nameofproduct"><%= product.name %></h1>
            <p id="descriptionofproduct"><%= product.description %></p>
            <p id="productcolor"><%= product.color %></p>
            <strong><%= product.price %></strong>
          </div>
        </div>
      </div><!-- /col -->
    <% end %>
  </div>
</div>

<br>

<%= link_to 'New Product', new_product_path %>

</br>

</body>